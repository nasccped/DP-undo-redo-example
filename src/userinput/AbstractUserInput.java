package userinput;

/**
 * Abstract empty class. This class is intended to have a lot of
 * variants (like an enum). These variants should be handled by
 * the MachineController class (machine's package). It'll modify
 * machine's inner attributes + update the report.
 *
 * It's empty 'cause we don't care about inner values, just the
 * inheritance.
 */
public abstract class AbstractUserInput {}
